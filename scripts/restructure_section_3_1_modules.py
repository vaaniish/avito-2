#!/usr/bin/env python3
# -*- coding: utf-8 -*-

from docx import Document
from docx.oxml import OxmlElement
from docx.text.paragraph import Paragraph


DOC_PATH = r"C:\Study\diploma\avito-2\PZ_updated_3_1_fixed.docx"


def remove_paragraph(paragraph):
    element = paragraph._element
    parent = element.getparent()
    if parent is not None:
        parent.remove(element)
    paragraph._p = paragraph._element = None


def insert_after(paragraph, text, style_name):
    new_p_elm = OxmlElement("w:p")
    paragraph._p.addnext(new_p_elm)
    new_p = Paragraph(new_p_elm, paragraph._parent)
    new_p.text = text
    new_p.style = style_name
    return new_p


def main():
    doc = Document(DOC_PATH)

    idx_31 = None
    idx_32 = None
    for i, p in enumerate(doc.paragraphs):
        t = (p.text or "").strip()
        if idx_31 is None and p.style.name == "2" and "Разработка бизнес-логики" in t:
            idx_31 = i
            continue
        if idx_31 is not None and p.style.name == "2" and i > idx_31:
            idx_32 = i
            break

    if idx_31 is None or idx_32 is None:
        raise RuntimeError("Не удалось найти границы подраздела 3.1.")

    idx_caption = None
    for i in range(idx_31, idx_32):
        t = (doc.paragraphs[i].text or "").strip()
        if t.startswith("Рисунок 3.1"):
            idx_caption = i
            break

    if idx_caption is None:
        raise RuntimeError("Не найдена подпись рисунка 3.1 в подразделе 3.1.")

    # Удаляем только содержимое после подписи рисунка и до следующего подраздела 3.2.
    for i in range(idx_32 - 1, idx_caption, -1):
        remove_paragraph(doc.paragraphs[i])

    cursor = doc.paragraphs[idx_caption]

    blocks = [
        ("text", "Далее приведено описание бизнес-логики по каждому backend-модулю системы."),
        ("h3", "Модуль аутентификации"),
        (
            "text",
            "Модуль обеспечивает регистрацию, вход и проверку прав доступа пользователей. "
            "На уровне бизнес-логики выполняется валидация учетных данных, формирование токенов сессии и "
            "проверка роли пользователя при обращении к защищенным маршрутам. "
            "Целостность данных поддерживается ограничениями уникальности email, username и public_id, "
            "а также контролем статуса учетной записи (например, запрет операций для пользователей со статусом BLOCKED).",
        ),
        ("h3", "Модуль каталога"),
        (
            "text",
            "Модуль каталога реализует операции просмотра и управления справочниками категорий, подкатегорий и товаров, "
            "а также публикацией объявлений. "
            "Бизнес-логика контролирует корректность связей между сущностями каталога и проверяет согласованность атрибутов объявления "
            "перед сохранением. "
            "На уровне БД применяются внешние ключи между категориями, подкатегориями и товарами, "
            "а также уникальные ограничения на наименования в пределах родительской сущности.",
        ),
        ("h3", "Модуль профиля и заказов"),
        (
            "text",
            "Модуль отвечает за профиль пользователя, адреса, корзину и жизненный цикл заказа от создания до завершения. "
            "В бизнес-логике реализованы правила переходов статусов заказа, проверка состава заказа, расчет сумм и обработка оплаты. "
            "Для адресов применяется правило единственного адреса по умолчанию на пользователя, "
            "что дополнительно фиксируется частичным уникальным ограничением. "
            "Интеграция с YooKassa API используется для выполнения платежных операций и синхронизации платежного статуса заказа.",
        ),
        ("h3", "Административный модуль"),
        (
            "text",
            "Административный модуль предоставляет сценарии управления системой: модерация пользователей, "
            "обработка жалоб и применение санкций. "
            "Бизнес-логика включает проверку полномочий администратора, ведение журналов действий и контроль корректности решений "
            "по жалобам и ограничениям. "
            "Через данный модуль обеспечивается централизованное управление критичными процессами платформы.",
        ),
        ("h3", "Модуль модерации объявлений"),
        (
            "text",
            "Модуль выполняет проверку объявлений перед публикацией и поддерживает процесс ручной модерации. "
            "В логике модуля фиксируются результаты проверки, причины отклонения и итоговые статусы объявлений. "
            "Обновление статуса объявления допускается только по допустимым сценариям, что предотвращает публикацию "
            "некорректного или несоответствующего правилам контента.",
        ),
        ("h3", "Партнерский модуль"),
        (
            "text",
            "Партнерский модуль предназначен для работы продавцов и внешних интеграций доставки. "
            "В модуле реализованы операции управления объявлениями продавца, обработка KYC-статусов и маршруты взаимодействия "
            "с сервисами доставки. "
            "При работе с доставкой используются проверки трек-номера и ограничение переходов по статусам отправления, "
            "а интеграция с API Почты России обеспечивает получение и актуализацию данных трекинга.",
        ),
        ("h3", "Ограничения целостности и проектные подходы"),
        (
            "text",
            "Ограничения целостности реализованы совместно на уровне БД и серверной логики: "
            "уникальные ключи, внешние ключи, ограничения по статусам и контроль допустимых переходов состояний. "
            "Архитектурно используется многослойная организация (маршруты, сервисная логика, доступ к данным), "
            "а слой Prisma выступает фасадом для централизованной работы с PostgreSQL. "
            "Полный SQL-скрипт структуры БД и ограничений целостности приведен в Приложении Б.",
        ),
    ]

    for kind, text in blocks:
        if kind == "h3":
            cursor = insert_after(cursor, text, "3")
        else:
            cursor = insert_after(cursor, text, "Деф текст")

    doc.save(DOC_PATH)
    print("OK")


if __name__ == "__main__":
    main()
