#!/usr/bin/env python3
# -*- coding: utf-8 -*-

from docx import Document
from docx.oxml import OxmlElement
from docx.text.paragraph import Paragraph


DOC_PATH = r"C:\Study\diploma\avito-2\PZ_updated_3_1_fixed.docx"


def remove_paragraph(paragraph):
    element = paragraph._element
    parent = element.getparent()
    if parent is not None:
        parent.remove(element)
    paragraph._p = paragraph._element = None


def insert_after(paragraph, text, style_name):
    new_p_elm = OxmlElement("w:p")
    paragraph._p.addnext(new_p_elm)
    new_p = Paragraph(new_p_elm, paragraph._parent)
    new_p.text = text
    new_p.style = style_name
    return new_p


def main():
    doc = Document(DOC_PATH)

    idx_31 = None
    idx_32 = None
    for i, p in enumerate(doc.paragraphs):
        t = (p.text or "").strip()
        if idx_31 is None and p.style.name == "2" and "Разработка бизнес-логики" in t:
            idx_31 = i
            continue
        if idx_31 is not None and p.style.name == "2" and i > idx_31:
            idx_32 = i
            break

    if idx_31 is None or idx_32 is None:
        raise RuntimeError("Не удалось определить границы подраздела 3.1.")

    idx_caption = None
    for i in range(idx_31, idx_32):
        t = (doc.paragraphs[i].text or "").strip()
        if t.startswith("Рисунок 3.1"):
            idx_caption = i
            break

    if idx_caption is None:
        raise RuntimeError("Не найдена подпись рисунка 3.1.")

    # Удаляем всё после подписи рисунка до начала 3.2.
    for i in range(idx_32 - 1, idx_caption, -1):
        remove_paragraph(doc.paragraphs[i])

    cursor = doc.paragraphs[idx_caption]

    blocks = [
        (
            "text",
            "После общей схемы взаимодействия модулей далее приведено детальное описание реализованной "
            "бизнес-логики серверной части по каждому модулю системы.",
        ),
        ("h3", "3.1.1 Модуль аутентификации"),
        (
            "text",
            "Модуль аутентификации реализует сценарии регистрации, входа и проверки текущей сессии "
            "пользователя. При регистрации выполняются нормализация e-mail, проверка обязательных полей, "
            "хеширование пароля и формирование публичного идентификатора пользователя. "
            "При авторизации проверяется соответствие пароля хешу и статус учетной записи.",
        ),
        (
            "text",
            "Для заблокированных пользователей реализована отдельная ветка бизнес-логики: если срок блокировки "
            "истек, учетная запись автоматически переводится в статус ACTIVE; при активной блокировке вход запрещается. "
            "Целостность учетных данных обеспечивается уникальными ограничениями на public_id, email и username, "
            "а разграничение доступа по ролям дополнительно контролируется на уровне middleware.",
        ),
        ("h3", "3.1.2 Модуль каталога"),
        (
            "text",
            "Модуль каталога обеспечивает чтение категорий и объявлений, поиск, получение карточки товара, "
            "вопросы по объявлению и подачу жалоб. В выборках каталога применяются фильтры по статусам "
            "(отображаются только ACTIVE и APPROVED), а также ограничение пагинации по limit/offset, "
            "что предотвращает неконтролируемую нагрузку на БД.",
        ),
        (
            "text",
            "Для карточек неактивных объявлений реализован контроль контекста доступа: такие данные могут получить "
            "только администратор, продавец объявления или пользователь с подтвержденной связью (заказ/избранное). "
            "В контуре жалоб реализованы антидублирование по окну времени, лимит частоты обращений и "
            "валидация содержания обращения. Для вопросов предусмотрено создание уведомления продавцу, "
            "что связывает модуль каталога с модулем коммуникаций/профиля.",
        ),
        ("h3", "3.1.3 Модуль профиля и заказов"),
        (
            "text",
            "Модуль профиля агрегирует персональные данные пользователя, адреса, заказы, избранное и уведомления. "
            "При работе с адресами поддерживается правило «один адрес по умолчанию», а в DTO выполняется нормализация "
            "и сборка полного адреса из структурированных полей. Для операций изменения профиля реализована "
            "валидация входных данных и проверка прав на редактирование только собственных данных.",
        ),
        (
            "text",
            "Ключевая бизнес-логика заказов включает формирование заказов по продавцам, расчет итоговой стоимости, "
            "доставки и скидок, а также создание финансовых транзакций платформы. Операции создания заказа и связанных "
            "платежных записей выполняются транзакционно через Prisma, что гарантирует согласованность данных. "
            "Для карточных платежей реализована интеграция с YooKassa (включая обработку сетевых ошибок и повторные "
            "попытки запроса), а для отзывов действует правило «только после завершенной покупки» и запрет дублирования "
            "отзыва одним пользователем.",
        ),
        ("h3", "3.1.4 Административный модуль"),
        (
            "text",
            "Административный модуль реализует управление пользователями, объявлениями, жалобами, KYC-заявками, "
            "комиссионными уровнями и аудитом действий. Доступ к маршрутам ограничен ролью ADMIN. "
            "Для каждой критичной операции (смена статуса жалобы, модерации, пользователя, KYC, комиссии) "
            "формируется запись в журнале аудита с фиксацией до/после изменения.",
        ),
        (
            "text",
            "В обработке жалоб реализован механизм эскалации санкций: WARNING, TEMP_3_DAYS, TEMP_30_DAYS, PERMANENT. "
            "При подтверждении нарушения автоматически деактивируется связанное объявление и назначается санкция продавцу, "
            "включая временную или постоянную блокировку. Использование транзакций и идемпотентных ключей в "
            "административных операциях предотвращает повторное применение одного и того же действия.",
        ),
        ("h3", "3.1.5 Модуль модерации объявлений"),
        (
            "text",
            "Модуль модерации реализован как отдельный слой правил, который оценивает содержимое объявления "
            "по набору формализованных признаков риска: контактные данные в тексте, попытки обхода платформы, "
            "спам-маркеры, признаки запрещенного контента, аномальные значения цены и другие сигналы.",
        ),
        (
            "text",
            "По результатам проверки формируется оценка риска и итоговое решение по статусам объявления "
            "(автопубликация либо перевод в ручную модерацию). Запуск проверки вынесен в фоновую задачу после "
            "создания/редактирования объявления, что уменьшает задержку ответа API и сохраняет целостность "
            "сценария публикации.",
        ),
        ("h3", "3.1.6 Партнерский модуль"),
        (
            "text",
            "Партнерский модуль обслуживает бизнес-процессы продавца: управление объявлениями, обработку заказов, "
            "ответы на вопросы покупателей и сопровождение доставки. Для объявлений реализованы разрешенные переходы "
            "статусов и запрет прямой активации продавцом без модерации. При изменении объявления автоматически "
            "сбрасывается статус модерации в PENDING, что исключает публикацию измененного контента без повторной проверки.",
        ),
        (
            "text",
            "В контуре заказов продавец может вручную менять только ограниченный набор статусов. После назначения "
            "трек-номера заказ переводится в SHIPPED, а дальнейшие изменения синхронизируются автоматически по данным "
            "службы доставки. Валидация трек-номера и получение внешнего статуса реализованы через интеграцию "
            "с API Почты России с fallback-проверками, что повышает устойчивость системы к отказам внешнего сервиса.",
        ),
        ("h3", "3.1.7 Ограничения целостности и проектные подходы"),
        (
            "text",
            "Ограничения целостности реализованы совместно на уровне схемы PostgreSQL и серверной бизнес-логики. "
            "На уровне БД применяются первичные/внешние ключи, уникальные ограничения (в том числе составные), "
            "индексы для критичных сценариев чтения, а также enum-типы для фиксированного набора допустимых статусов.",
        ),
        (
            "text",
            "На уровне приложения дополнительно проверяются инварианты предметной области: допустимые переходы "
            "статусов заказов и объявлений, правила ролей, условия публикации и ограничения пользовательских действий. "
            "Архитектурно используется модульное разбиение по доменам и централизованный слой доступа к данным "
            "через Prisma (паттерн «Фасад»). Полный SQL-скрипт структуры БД и ограничений целостности приведен "
            "в Приложении Б.",
        ),
    ]

    for kind, text in blocks:
        if kind == "h3":
            cursor = insert_after(cursor, text, "3")
        else:
            cursor = insert_after(cursor, text, "Деф текст")

    doc.save(DOC_PATH)
    print("OK")


if __name__ == "__main__":
    main()
