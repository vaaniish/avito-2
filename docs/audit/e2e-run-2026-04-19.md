# E2E Runtime Report (2026-04-19)

Date: 2026-04-19  
Environment: local Docker + backend on `http://localhost:3001`

## Command

- `node scripts/e2e-smoke.mjs`

## Result

- Total checks: `12`
- Passed: `12`
- Failed: `0`

## Notes

- Smoke script now reads `.env` (`dotenv/config`) and uses the same `DATABASE_URL` as runtime, eliminating false DB-mismatch failures.
- Backend runtime still emits an internal pg deprecation warning (`client.query while already executing query`) and should be inspected in a separate perf/concurrency pass.
