# E2E Runtime Report (2026-04-18)

Date: 2026-04-18  
Environment: local Docker + test DB `avito-db-ci` on `localhost:5433`

## Setup used

- `docker compose up -d db`
- `DATABASE_URL=postgresql://user:password@localhost:5433/avito-db-ci?schema=public`
- `prisma db push` to `avito-db-ci`
- seed: `node node_modules/tsx/dist/cli.mjs backend/prisma/seed.ts`
- backend started with:
  - `DATABASE_URL=.../avito-db-ci?schema=public`
  - `MODERATION_AI_PROVIDER=none`
- smoke runner:
  - `node scripts/e2e-smoke.mjs`

## Automated smoke result

- Total checks: `12`
- Passed: `12`
- Failed: `0`

### Covered areas

1. Health endpoint
2. Auth login + `/auth/me`
3. Catalog list/detail/suggestions
4. Profile data + notifications mark-as-read
5. Address CRUD + default switching + DB verification
6. Wishlist add/remove + API verification
7. Partner read endpoints
8. Partner listing create/update/toggle/delete + DB verification
9. Admin read endpoints
10. Admin mutations (`users`, `listings moderation`, `commission`, `complaints`) + audit growth verification in DB
11. Order create path behavior with missing YooKassa credentials

## UI route smoke

Via `vite preview` on `http://127.0.0.1:4173`:

- `GET /` -> `200`
- `GET /auth` -> `200`
- `GET /profile` -> `200`
- `GET /admin` -> `200`
- `GET /products/LST-001` -> `200`
- `GET /sellers/SLR-001` -> `200`
- `GET /checkout` -> `200`

All checked routes return SPA shell (`<div id="root">` present).

## Known runtime limitation

- Checkout order creation with payment currently returns expected provider-related failure when YooKassa env is not configured.
- Endpoint behavior is handled and surfaced as a meaningful error (no crash).

## Notes

- Existing default DB `avito-db` had legacy schema drift, so tests were executed against isolated DB `avito-db-ci` to avoid destructive reset and data loss.
