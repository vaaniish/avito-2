# -*- coding: utf-8 -*-
from docx import Document
from docx.oxml import OxmlElement
from docx.text.paragraph import Paragraph

SRC = r"C:\Study\diploma\avito-2\PZ_updated_3_2_draft_v4.docx"
OUT = r"C:\Study\diploma\avito-2\PZ_updated_3_2_draft_v5.docx"


def insert_after(paragraph, text, style=None):
    new_p = OxmlElement('w:p')
    paragraph._p.addnext(new_p)
    new_para = Paragraph(new_p, paragraph._parent)
    if style is not None:
        new_para.style = style
    if text:
        new_para.add_run(text)
    return new_para


doc = Document(SRC)

start = None
end = None
for i, p in enumerate(doc.paragraphs):
    t = (p.text or '').strip()
    if t.startswith('3.2.2 '):
        start = i
    if start is not None and i > start and t.startswith('3.2.3 '):
        end = i
        break

if start is None or end is None:
    raise RuntimeError(f'Не найдены границы подраздела 3.2.2: start={start}, end={end}')

# Remove current body of 3.2.2
for i in range(end - 1, start, -1):
    p = doc.paragraphs[i]
    p._element.getparent().remove(p._element)

anchor = doc.paragraphs[start]
style_normal = anchor.style

lines = [
    'На этапе пользовательских сценариев были формализованы целевые действия всех ролевых групп системы и выполнена декомпозиция каждого действия на последовательность интерфейсных шагов. Сценарии фиксировались в формате «цель -> шаги -> результат», что позволило проверить избыточные переходы и сократить длину пользовательского пути в критичных операциях.',
    'Всего сформировано 16 сценариев, объединенных в 4 подгруппы: покупательский контур, личный кабинет пользователя, партнерский контур и административный контур. Такое группирование соответствует фактической модульной структуре интерфейса и ролям доступа, реализованным в системе.',
    '3.2.2.1 Подгруппа «Покупательский контур»',
    'В подгруппу включены сценарии S1-S4: поиск и фильтрация объявлений, оформление заказа через корзину, быстрый сценарий «Купить сейчас», работа с избранным. Для этой подгруппы ключевыми метриками являются минимальное число шагов до целевого действия, прозрачность checkout и сохранение контекста товара при переходах между каталогом, карточкой и оформлением.',
    '[Вставить изображение: report_assets/scenarios/group_1_buyer.png]',
    'Рисунок 3.3 – Пользовательские сценарии подгруппы «Покупательский контур»',
    '3.2.2.2 Подгруппа «Личный кабинет пользователя»',
    'В подгруппу включены сценарии S5-S8: регистрация и вход, редактирование профиля, добавление адреса доставки через карту, просмотр истории заказов и оставление отзыва. В данной группе проверялась целостность персональных данных, непрерывность пользовательского контекста после сохранения форм и единообразие обратной связи при успешных и ошибочных операциях.',
    '[Вставить изображение: report_assets/scenarios/group_2_profile.png]',
    'Рисунок 3.4 – Пользовательские сценарии подгруппы «Личный кабинет пользователя»',
    '3.2.2.3 Подгруппа «Партнерский контур»',
    'В подгруппу включены сценарии S9-S12: создание объявления через мастер, редактирование и управление статусом объявления, ответы на вопросы покупателей, обработка заказа и назначение трек-номера. Основная цель оптимизации этой группы — снижение операционной нагрузки при регулярной работе партнера и исключение лишних переходов между связанными действиями.',
    '[Вставить изображение: report_assets/scenarios/group_3_partner.png]',
    'Рисунок 3.5 – Пользовательские сценарии подгруппы «Партнерский контур»',
    '3.2.2.4 Подгруппа «Административный контур»',
    'В подгруппу включены сценарии S13-S16: вход в административную панель, обработка жалоб, модерация объявлений, управление пользователями и KYC-заявками. Для этой группы приоритетными являются однозначность статусов, контролируемость критичных действий и соблюдение ролевых ограничений интерфейса.',
    '[Вставить изображение: report_assets/scenarios/group_4_admin.png]',
    'Рисунок 3.6 – Пользовательские сценарии подгруппы «Административный контур»',
    'Результаты этапа пользовательских сценариев использованы как входные данные для этапа 3.2.3 (структура интерфейса): на основе шагов сценариев были зафиксированы маршруты переходов, точки входа в разделы и требования к состояниям форм и уведомлений.'
]

for line in lines:
    anchor = insert_after(anchor, line, style=style_normal)

# Shift subsequent figure numbers to avoid duplicates with new 3.3-3.6
keep = {
    'Рисунок 3.3 – Пользовательские сценарии подгруппы «Покупательский контур»',
    'Рисунок 3.4 – Пользовательские сценарии подгруппы «Личный кабинет пользователя»',
    'Рисунок 3.5 – Пользовательские сценарии подгруппы «Партнерский контур»',
    'Рисунок 3.6 – Пользовательские сценарии подгруппы «Административный контур»',
}

prefix = 'Рисунок 3.'
for i, p in enumerate(doc.paragraphs):
    t = (p.text or '').strip()
    if i <= start or not t.startswith(prefix) or t in keep:
        continue

    rest = t[len(prefix):]
    j = 0
    while j < len(rest) and rest[j].isdigit():
        j += 1
    if j == 0:
        continue

    num = int(rest[:j])
    if num >= 4:
        p.text = f'{prefix}{num + 3}{rest[j:]}'


doc.save(OUT)
print(OUT)
